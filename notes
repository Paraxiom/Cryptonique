 src/encryption/
- src/decryption/
- src/encoding/
- src/spatial_pooling/
- src/temporal_keys.rs
- src/transaction/
security and obfuscation never works that well
machine code reveals secrets
hack of one key should never lead to the loss of other key
use session keys not long term keys
Entropy
Understan strenchghts and weaknesses
Thest for out of bands conditions
zero is not your friend
Real random
  key derivation function
  Salt values
  Everything below 72 bits is crackable
  nonce/salt should start at 96 bits
  Never reuse salt values
  Risk register
  